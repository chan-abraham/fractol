//this will be the file that will hold the multithread functions
//here, we will be using pthread, at the end of which, will send it off
//to a function pointer which will send it off to the fractal functions